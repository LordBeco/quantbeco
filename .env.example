# =============================================================================
# TRADE ANALYZER PRO - ENVIRONMENT CONFIGURATION
# =============================================================================
# This file contains all environment variables used by the Trade Analyzer Pro
# Copy this file to .env and configure the values according to your needs
# =============================================================================

# =============================================================================
# AI PROVIDER CONFIGURATION
# =============================================================================
# Choose your AI provider: 'puter' (free), 'openrouter' (free models available), or 'openai' (paid)
AI_PROVIDER=puter

# =============================================================================
# OPENAI CONFIGURATION (Premium AI Provider)
# =============================================================================
# Required for OpenAI provider: Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# OpenAI model configuration
OPENAI_MODEL=gpt-4
OPENAI_MAX_TOKENS=2000
OPENAI_TEMPERATURE=0.1

# =============================================================================
# PUTER AI CONFIGURATION (Free AI Provider)
# =============================================================================
# Puter AI is a free alternative that doesn't require an API key
PUTER_MODEL=gpt-5-nano
PUTER_MAX_TOKENS=2000
PUTER_TEMPERATURE=0.1

# =============================================================================
# OPENROUTER CONFIGURATION (Free Models Available)
# =============================================================================
# OpenRouter API key (REQUIRED - even for free models)
# Get your FREE API key from https://openrouter.ai/keys
OPENROUTER_API_KEY=your_openrouter_api_key_here

# OpenRouter model configuration (free model by default)
OPENROUTER_MODEL=nex-agi/deepseek-v3.1-nex-n1:free
OPENROUTER_MAX_TOKENS=2000
OPENROUTER_TEMPERATURE=0.1

# OpenRouter application identification
OPENROUTER_SITE_URL=http://localhost:8501
OPENROUTER_SITE_NAME=Trade Analyzer Pro

# =============================================================================
# BACKTESTING CONFIGURATION
# =============================================================================
# Default parameters for backtesting engine
DEFAULT_STARTING_BALANCE=10000
DEFAULT_LEVERAGE=1.0
DEFAULT_COMMISSION=0.0
DEFAULT_SLIPPAGE=0.0

# =============================================================================
# DATA PROCESSING CONFIGURATION
# =============================================================================
# Maximum file upload size in megabytes
MAX_UPLOAD_SIZE_MB=2048

# Default timezone for data processing
DEFAULT_TIMEZONE=UTC

# =============================================================================
# DEPLOYMENT CONFIGURATION (Heroku/Cloud Platforms)
# =============================================================================
# Port for web server (automatically set by cloud platforms)
# PORT=8507

# =============================================================================
# STREAMLIT CONFIGURATION
# =============================================================================
# These are typically set automatically by Streamlit or deployment platforms
# Uncomment and modify if needed for custom deployments

# STREAMLIT_SERVER_PORT=8507
# STREAMLIT_SERVER_ADDRESS=0.0.0.0
# STREAMLIT_SERVER_HEADLESS=true
# STREAMLIT_SERVER_ENABLE_CORS=false

# =============================================================================
# DEVELOPMENT CONFIGURATION
# =============================================================================
# Set to 'development' for debug mode, 'production' for deployment
# ENVIRONMENT=development

# Enable debug logging
# DEBUG=false

# =============================================================================
# NOTES
# =============================================================================
# 1. AI Provider Selection:
#    - 'puter': Free, no API key required, template-based responses
#    - 'openrouter': Free models available, but API key required (free to get)
#    - 'openai': Premium service, requires API key and credits
#
# 2. For production deployment:
#    - Set strong values for all configuration parameters
#    - Use environment-specific settings
#    - Keep API keys secure and never commit them to version control
#
# 3. File Upload Limits:
#    - Adjust MAX_UPLOAD_SIZE_MB based on your server capabilities
#    - Consider memory usage for large CSV files
#
# 4. Timezone Configuration:
#    - Use standard timezone names (UTC, US/Eastern, Europe/London, etc.)
#    - Affects timestamp processing and chart displays